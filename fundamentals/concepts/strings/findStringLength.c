#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{

    char S[] = "Welcome";
    int i;
    for (i = 0; S[i] != '\0'; i++) // \0 means NULL terminator
    {
    };
    // Built-in function to find the length of a string
    // strLen(S);

    printf("The length of string: %d\n", i);
    return 0;
}
